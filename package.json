{
  "name": "teloscode-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:server && node scripts/prerender.mjs",
    "build:server": "vite build --ssr src/entry-server.jsx --outDir dist/server",
    "images": "node scripts/optimize-images.mjs",
    "preview": "vite preview",
    "og": "node scripts/generate-og.mjs",
    "fonts": "node scripts/fetch-fonts.mjs"
  },
  "dependencies": {
    "posthog-js": "^1.414.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^5.4.10"
  }
}
